# Approach Create Logos Design

## Goal

Refine the Create evidence state on the MA presentation's Approach slide by replacing the three selected feature names with their transparent logo assets.

## Visual treatment

- Keep `Daily Memory Card` and `Pet Equips` as struck-through text.
- Replace the `Hometown`, `Card Bounty`, and `Hot Trail` text headings with their corresponding assets from `public/coinmaster/`.
- Normalize the logos by height while preserving each asset's natural aspect ratio and transparent background.
- Keep each feature's monetization sentence directly beneath its logo.
- Remove the top border above all five concept items.
- Preserve the current five-column layout, reveal transition, and accessibility by giving every logo its feature name as alt text.

## Verification

The presentation test must assert the three logo sources, accessible alt text, retained summaries, and absence of top-border classes in the Create evidence items.
